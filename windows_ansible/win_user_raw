- hosts: all
  tasks:
  - name: List user accounts on a Windows system
    raw: Get-WmiObject -Class Win32_UserAccount
    register: h

  - debug: msg="{{ h.stdout }}"
  - debug: msg=all
  - debug: msg="{{ h }}"
